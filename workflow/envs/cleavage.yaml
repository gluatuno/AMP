name: amp-cleavage
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.10
  - pandas>=2.0
  - biopython>=1.81
  - numpy>=1.24
  - scikit-learn>=1.3   # logistic-regression cut-site classifier (optional; PWM fallback otherwise)
  - meme>=5.5           # STREME/MEME for de-novo cut-site motif discovery
